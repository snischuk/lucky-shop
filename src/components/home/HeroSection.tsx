import Button from '../ui/Button';

const HeroSection = () => (
  <section className="text-center py-16 bg-gray-100">
    <h1 className="text-4xl font-bold mb-4">Вітаємо!</h1>
    <p className="text-lg mb-6">Наша платформа — найкраще місце для початку.</p>
    <Button>Почати</Button>
  </section>
);

export default HeroSection;
