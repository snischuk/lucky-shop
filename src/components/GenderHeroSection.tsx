import type { FC } from 'react';

const GenderHeroSection: FC = () => {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center">
      <h1 className="text-2xl font-bold">Gender Hero Section</h1>
    </div>
  );
};

export { GenderHeroSection };
